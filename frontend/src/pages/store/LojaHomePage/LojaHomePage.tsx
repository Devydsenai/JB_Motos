import { PagePlaceholder } from "@components/molecules/PagePlaceholder";

/** Área da loja — outro dev implementará o catálogo e checkout */
export function LojaHomePage() {
  return (
    <PagePlaceholder
      title="Loja JB Motos"
      description="Área do cliente: cadastro, marca da moto e solicitação de peças. Esta seção será desenvolvida pela equipe da loja."
    />
  );
}
