import { PagePlaceholder } from "@components/molecules/PagePlaceholder";

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Visão geral: pedidos, serviços, estoque e indicadores financeiros (dados virão do backend)."
    />
  );
}
